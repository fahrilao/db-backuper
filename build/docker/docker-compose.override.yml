version: "3.1"

services:
  dbbackup:
    command: yarn start
    build:
      context: ../../
      dockerfile: ./build/docker/dockerfile
      target: builder
    networks:
      - himed_network

networks:
  himed_network:
    external: true
